// Demo file created by Claude Code
// This demonstrates the Write tool capability

export function greet(name: string): string {
  return `Hello, ${name}!`;
}

export function add(a: number, b: number): number {
  return a + b;
}
